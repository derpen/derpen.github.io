# The Staircase

A portofolio presented as a 3D horror game

Currently using Three.js

Would love to learn barebone WebGL


## Usage

`npx vite` for development

`npx vite build` for building. Files will be located in `dist/`
